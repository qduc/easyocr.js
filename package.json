{
  "name": "easyocr.js",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.4",
  "description": "JS variant of EasyOCR",
  "workspaces": [
    "packages/*",
    "examples/*",
    "benchmarks/*"
  ],
  "scripts": {
    "test": "bun run -F @qduc/easyocr-core test && bun run -F @qduc/easyocr-node test && bun run -F @qduc/easyocr-web test",
    "build": "bun run -F @qduc/easyocr-core build && bun run -F @qduc/easyocr-node build && bun run -F @qduc/easyocr-web build",
    "release": "bun run scripts/release.ts"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite-tsconfig-paths": "^6.0.5"
  }
}
